package Doit_알고리즘_코딩테스트.정렬.퀵정렬;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.security.Key;
import java.util.StringTokenizer;

public class P11004_K번째수구하기 {
  /*  public static void main(String[] args) throws Exception{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());
        int K = Integer.parseInt(br.readLine());
        int[] A = new int[N];

        StringTokenizer st = new StringTokenizer(br.readLine());

        for (int i = 0; i < N; i++) {
            A[i] = Integer.parseInt(st.nextToken());
        }

    }

    public static int quickSort(int start, int end, int K) {
        int p = pivot(start, end);
        if (p == K) {
            return K;
        } else if (K < p) {
            return quickSort(start, p - 1, K);
        } else {
            return quickSort(p + 1, end, K);
        }
    }

    public static int pivot(int start, int end) {

    }*/
}
